<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Happy Birthday Miimi 💖</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: radial-gradient(circle at center, #ffb6c1, #ff7f94, #ff4d6d);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Poppins', sans-serif;
      overflow: hidden;
      color: #fff;
    }

    .container { text-align: center; z-index: 2; position: relative; }
    
    button {
    background-color: white;
    color: #ff4d6d;
    border: none;
    padding: 16px 32px;
    border-radius: 12px;
    font-size: 1.2em;
    font-weight: bold;
    cursor: pointer;
    box-shadow: 0 0 20px rgba(255, 77, 109, 0.6), 0 0 40px rgba(255, 77, 109, 0.3);
    transition: transform 0.2s ease;
    animation: pulseGlow 2s infinite;
    }

    button:hover {
    transform: scale(1.08);
    box-shadow: 0 0 25px rgba(255, 77, 109, 0.8), 0 0 60px rgba(255, 77, 109, 0.5);
    }

    @keyframes pulseGlow {
    0% { box-shadow: 0 0 15px rgba(255, 77, 109, 0.5), 0 0 30px rgba(255, 77, 109, 0.2); }
    50% { box-shadow: 0 0 30px rgba(255, 77, 109, 0.9), 0 0 60px rgba(255, 77, 109, 0.5); }
    100% { box-shadow: 0 0 15px rgba(255, 77, 109, 0.5), 0 0 30px rgba(255, 77, 109, 0.2); }
    }


    h1 { font-size: 2.5em; margin-bottom: 20px; }

    .message {
      display: none;
      font-size: 1.3em;
      max-width: 600px;
      margin: 0 auto;
      line-height: 1.5;
      animation: fadeIn 1s forwards;
      background: rgba(255,255,255,0.1);
      padding: 20px 25px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.2);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }

    canvas { position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; }

    .heart {
      position: fixed;
      color: #fff;
      font-size: 24px;
      opacity: 0.8;
      animation: floatUp 6s linear infinite;
    }

    @keyframes floatUp {
      0% { transform: translateY(0) scale(1); opacity: 0.9; }
      100% { transform: translateY(-100vh) scale(1.5); opacity: 0; }
    }
  </style>
</head>
<body>
  <canvas id="confetti"></canvas>
  <div class="container">
    <h1>Hey Miimi 💖</h1>
    <button id="clickBtn">Click Me 🎁</button>

    <div class="message" id="message">
      🎂 <b>Happy Birthday, my love!</b> 🎂<br><br>
      Today is all about celebrating <b>YOU</b> a truly special person in my life,with the kindest heart and the brightest smile.  
      I just want you to know how much I love you, and how proud I am of everything you are and all that you’re becoming. 💕  
      <br><br>
      I miss you so so much and I wish i could be with you on this day!!
      <br><br>
      Enjoy this day to the fullest, because you deserve all the happiness in the world.  
      The best is yet to come, my love and I can’t wait to see you shine even more. 🌹
      
      <br><br>
      I love you so so much Bafanas,<br>
      <b>MAX❤️</b>
    </div>
  </div>

  <script>
    const btn = document.getElementById('clickBtn');
    const msg = document.getElementById('message');
    const canvas = document.getElementById('confetti');
    const ctx = canvas.getContext('2d');
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    // 🎊 Confetti animation
    function confetti(){
      const confs = [];
      for(let i=0;i<200;i++){
        confs.push({
          x: Math.random()*canvas.width,
          y: Math.random()*canvas.height - canvas.height,
          r: Math.random()*6+2,
          d: Math.random()*0.2+0.3,
          color: `hsl(${Math.random()*360}, 100%, 70%)`
        });
      }
      function draw(){
        ctx.clearRect(0,0,canvas.width,canvas.height);
        for(let c of confs){
          ctx.beginPath();
          ctx.arc(c.x,c.y,c.r,0,2*Math.PI);
          ctx.fillStyle=c.color;
          ctx.fill();
        }
        update();
        requestAnimationFrame(draw);
      }
      function update(){
        for(let c of confs){
          c.y += c.d*10;
          if(c.y>canvas.height) c.y = -10;
        }
      }
      draw();
    }

    // 💕 Floating hearts
    function createHeart(){
      const heart = document.createElement('div');
      heart.classList.add('heart');
      heart.style.left = Math.random()*100 + 'vw';
      heart.style.animationDuration = (4 + Math.random()*3) + 's';
      heart.innerText = ['💖','💘','💝','❤️','💕'][Math.floor(Math.random()*5)];
      document.body.appendChild(heart);
      setTimeout(()=>heart.remove(), 6000);
    }

    // 💫 Main click event
    btn.addEventListener('click', ()=>{
      btn.style.display = 'none';
      msg.style.display = 'block';
      confetti();
      setInterval(createHeart, 300);
      const audio = new Audio("https://actions.google.com/sounds/v1/ambiences/celebration_horn.ogg");
      audio.play();
    });

    window.addEventListener('resize', () => {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    });
  </script>
</body>
</html>
